export const metadata = {
  title: 'Caja Prestamos',
  description: 'App de prestamos',
}

export default function RootLayout({ children }) {
  return (
    <html lang="es">
      <body>{children}</body>
    </html>
  )
}
